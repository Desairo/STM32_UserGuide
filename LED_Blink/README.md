# LED Blink
1)To make it possible led.c and led.h files need to be created and used.  
2)Initially we use led_init() function to initialise LED things.  
3)Then we will use led_on() function after which a delay is added in it.  
4)
