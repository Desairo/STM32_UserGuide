# GPIO Register
1)GPIO used for many purposes so called general purpose registers.Some pins can't be used for all purposes.  
2)GPIO can be input, output, alternate function or analog.When MCU pin is in input mode it can be configured to issue an interrupt to processor.  
3)When input driver operates at that time output control will be deactivated.  


